# TaskFlow frontend environment template.
# Copy to .env and fill in real values. NEVER commit real secrets.
# TODO: Samira Patel — expand with all Sprint 1 variables.

# Backend API base URL (proxied in dev, real URL in prod)
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_URL=http://localhost:8000

# TODO: Add auth config, feature flags, analytics keys as needed.
